const m = (function () {
  // Email Messages
  const EMAIL_SENT_MESSAGE = "Email successfully sent";

  return (function () {
    return {
      email_sent_message: EMAIL_SENT_MESSAGE,
    };
  })();
})();

export default m;
