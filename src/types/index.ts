export interface objectEmailAndPortType {
  emailHost: string;
  emailPort: number;
}

export interface objectEmailBody {
  emailSentTo: string;
  emailSubject: string;
  emailText: string;
  emailHTML: string;
}
